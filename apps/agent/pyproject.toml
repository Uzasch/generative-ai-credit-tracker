[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Uzasch", email = "uzair2021.cheulkar@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "langgraph>=1.2.10",
]

[project.scripts]
agent = "agent:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
